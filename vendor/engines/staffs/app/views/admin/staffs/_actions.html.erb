<ul>
  <% if Admin::StaffsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_staffs_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_staff_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::StaffsController.sortable? and Staff.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Staffs"),
                 admin_staffs_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Staffs"),
                 admin_staffs_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>